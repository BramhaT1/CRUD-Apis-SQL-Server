

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}




model InternTesting {
  name String
  id   Int @id @default(autoincrement())
}
