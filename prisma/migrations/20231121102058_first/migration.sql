BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[InternTesting] (
    [name] NVARCHAR(1000) NOT NULL,
    [id] INT NOT NULL,
    CONSTRAINT [InternTesting_pkey] PRIMARY KEY CLUSTERED ([id])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
